import React from 'react';

const Portfolio = () => {
    return (
        <section className='full_size portfolio_page--container' tabIndex={2} id="portfolio">
            Portfolio
        </section>
    );
}

export default Portfolio;
